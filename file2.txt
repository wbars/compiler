program binary_search;

var
  arr: array[0..8] of Integer;
  mid,ilow,ihigh,search: Integer;
  i,n,fi, j: Integer;
  found: Boolean;
begin
  for j := 0 to 10 do begin
      writeln(j);
      if j > 5 then break;
    end;
end.